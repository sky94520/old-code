#! /usr/bin/python
# -*- coding: gbk -*-

import os;

for m in range(0,10):
             startName = raw_input("请输入文件开始名称,");
             counts = raw_input("文件数量");
             j = raw_input("增长量");
             prefix=raw_input("文件前缀");
             for i in range(0,counts):
                          fileName = int(startName)
                          os.rename();
